gzip on;
gzip_comp_level 6;
server {
    listen 80;

    location /api {
        proxy_set_header Host $http_host;
        proxy_pass http://localhost:8080;
    }

    location / {
        root /opt/gsati/ui;
        try_files $uri $uri/ index.html;
    }
}