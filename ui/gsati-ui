server {
    listen 80;

    location ^~ /api {
        rewrite ^/api(.*)$ $1 break;
        proxy_set_header X-Api-Root $scheme://$host:$server_port/api;
        proxy_pass http://api:8080;
    }

    location / {
        root /opt/gsati/ui;
        try_files $uri $uri/ index.html;
    }
}